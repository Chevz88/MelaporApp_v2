<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Aplikasi Theme -->
    <style name="Theme.MelaporApp" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Warna Utama -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>

        <!-- Warna Background & Permukaan -->
        <item name="android:windowBackground">@color/colorBackground</item>
        <item name="colorSurface">@color/colorSurface</item>
        <item name="android:colorBackground">@color/colorBackground</item>

        <!-- Warna Teks -->
        <item name="android:textColor">@color/colorOnBackground</item>
        <item name="colorOnPrimary">@color/colorOnPrimary</item>
        <item name="colorOnBackground">@color/colorOnBackground</item>
        <item name="colorOnSurface">@color/colorOnSurface</item>

        <!-- Status Bar -->
        <item name="android:statusBarColor">@color/colorPrimaryDark</item>
        <item name="android:windowLightStatusBar">false</item>

        <!-- Komponen - FIXED: Hapus textViewStyle yang bermasalah -->
        <item name="buttonStyle">@style/AppButtonStyle</item>
        <item name="editTextStyle">@style/AppEditTextStyle</item>
        
        <!-- Gunakan android:textViewStyle untuk compatibility yang lebih baik -->
        <item name="android:textViewStyle">@style/AppTextStyle</item>
    </style>

    <!-- Gaya Button -->
    <style name="AppButtonStyle" parent="Widget.AppCompat.Button">
        <item name="android:backgroundTint">@color/colorPrimary</item>
        <item name="android:textColor">@color/white</item>
        <item name="android:padding">12dp</item>
        <item name="android:textStyle">bold</item>
        <item name="android:textSize">16sp</item>
    </style>

    <!-- Gaya EditText -->
    <style name="AppEditTextStyle" parent="Widget.AppCompat.EditText">
        <item name="android:background">@drawable/bg_input</item>
        <item name="android:textColor">@color/inputText</item>
        <item name="android:padding">12dp</item>
        <item name="android:textSize">14sp</item>
    </style>

    <!-- FIXED: Rename dari AppTextStyle.Base ke AppTextStyle -->
    <style name="AppTextStyle" parent="TextAppearance.AppCompat">
        <item name="android:textColor">@color/colorOnBackground</item>
        <item name="android:textSize">14sp</item>
    </style>

    <!-- Untuk manual penggunaan -->
    <style name="AppTextStyle.Title" parent="AppTextStyle">
        <item name="android:textSize">20sp</item>
        <item name="android:textStyle">bold</item>
        <item name="android:textColor">@color/black</item>
    </style>

    <style name="AppTextStyle.Subtitle" parent="AppTextStyle">
        <item name="android:textSize">16sp</item>
        <item name="android:textStyle">normal</item>
        <item name="android:textColor">@color/gray_dark</item>
    </style>

</resources>
