<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Tema Utama Aplikasi -->
    <style name="Theme.MelaporApp" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Warna Brand -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>

        <!-- Warna Background dan Permukaan -->
        <item name="android:windowBackground">@color/colorBackground</item>
        <item name="colorSurface">@color/colorSurface</item>
        <item name="android:colorBackground">@color/colorBackground</item>

        <!-- Warna Teks -->
        <item name="android:textColor">@color/colorOnBackground</item>
        <item name="colorOnPrimary">@color/colorOnPrimary</item>
        <item name="colorOnBackground">@color/colorOnBackground</item>
        <item name="colorOnSurface">@color/colorOnSurface</item>

        <!-- Status Bar -->
        <item name="android:statusBarColor">@color/colorPrimaryDark</item>
        <item name="android:windowLightStatusBar">false</item>

        <!-- Default Komponen -->
        <item name="buttonStyle">@style/AppButtonStyle</item>
        <item name="textViewStyle">@style/AppTextStyle</item>
        <item name="editTextStyle">@style/AppEditTextStyle</item>
    </style>

    <!-- Gaya Tombol -->
    <style name="AppButtonStyle" parent="Widget.AppCompat.Button">
        <item name="android:backgroundTint">@color/colorPrimary</item>
        <item name="android:textColor">@color/white</item>
        <item name="android:padding">12dp</item>
        <item name="android:textStyle">bold</item>
        <item name="android:textSize">16sp</item>
    </style>

    <!-- Gaya EditText -->
    <style name="AppEditTextStyle" parent="Widget.AppCompat.EditText">
        <item name="android:background">@drawable/bg_input</item> <!-- atau warna solid jika belum ada -->
        <item name="android:textColor">@color/inputText</item>
        <item name="android:padding">12dp</item>
        <item name="android:textSize">14sp</item>
    </style>

    <!-- Gaya TextView Default -->
    <style name="AppTextStyle" parent="Widget.AppCompat.TextView">
        <item name="android:textColor">@color/colorOnBackground</item>
        <item name="android:textSize">14sp</item>
    </style>

    <!-- Teks Judul -->
    <style name="AppTextStyle.Title" parent="TextAppearance.AppCompat.Large">
        <item name="android:textSize">20sp</item>
        <item name="android:textStyle">bold</item>
        <item name="android:textColor">@color/black</item>
    </style>

    <!-- Teks Subjudul -->
    <style name="AppTextStyle.Subtitle" parent="TextAppearance.AppCompat.Medium">
        <item name="android:textSize">16sp</item>
        <item name="android:textStyle">normal</item>
        <item name="android:textColor">@color/gray_dark</item>
    </style>

</resources>
